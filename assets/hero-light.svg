<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 186"
     width="1200" height="186" role="img" aria-label="What's On Netflix">
  <style>
    /* Entrance animations move things but never fade them. A renderer that does
       not run CSS - GitHub's mobile app, an email digest, a PDF export - would
       otherwise show an empty banner, because the from-state of a fade is
       invisible and fill-mode `both` holds it for the whole delay. Every element
       here is fully legible with the animation stripped out. */
    .rise  { animation: rise .55s cubic-bezier(.2,.7,.3,1) both; }
    @keyframes rise { from { transform: translateY(9px) } to { transform: translateY(0) } }
    @media (prefers-reduced-motion: reduce) { * { animation: none !important } }
    .sweep { animation: sweep .9s cubic-bezier(.2,.7,.3,1) .15s; }
    .blob  { animation: drift 14s ease-in-out infinite alternate; }
    @keyframes sweep { from { width: 0 } to { width: 1120px } }
    @keyframes drift { from { transform: translate(0,0) }
                        to   { transform: translate(64px,-22px) } }
  </style>
  <defs>
    <radialGradient id="g" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#E50914" stop-opacity="0.07"/>
      <stop offset="100%" stop-color="#E50914" stop-opacity="0"/>
    </radialGradient>
  </defs>
  <!-- The panel paints the theme's own card colour rather than staying
       transparent, so the hero reads as a deliberate surface on all four
       GitHub backgrounds instead of borrowing whichever one sits behind it. -->
  <rect width="1200" height="186" rx="16" fill="#F6F8FA"/>
  <rect x="0.5" y="0.5" width="1199" height="185" rx="15.5" fill="none" stroke="#D0D7DE"/>
  <rect width="8" height="186" rx="4" fill="#E50914"/>
  <g class="blob"><circle cx="1010" cy="70" r="240" fill="url(#g)"/></g>
  <text class="rise" x="40" y="74" font-family="system-ui,-apple-system,'Segoe UI',Roboto,Helvetica,Arial,sans-serif" font-size="22" font-weight="700"
        letter-spacing="3" fill="#E50914">8,804 TITLES  ·  122 COUNTRIES  ·  42 GENRES</text>
  <rect class="sweep" x="40" y="108" width="1120" height="2"
        rx="1" fill="#D0D7DE"/>
  <text class="rise" style="animation-delay:0.20s" x="40" y="146"
        font-family="system-ui,-apple-system,'Segoe UI',Roboto,Helvetica,Arial,sans-serif" font-size="25" fill="#656D76">One Kaggle CSV, kept as diffable PBIP text - not a binary.</text>
</svg>
